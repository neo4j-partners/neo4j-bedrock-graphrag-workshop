[project]
name = "export-graph"
version = "0.1.0"
description = "One-shot export of the gold Neo4j database to TransformedData CSVs"
requires-python = ">=3.11"
dependencies = [
    "neo4j>=5.0.0",
    "python-dotenv>=1.0.0",
]

[tool.hatch.build.targets.wheel]
packages = ["."]
