= Lab 6: Neo4j MCP Agent

Earlier labs used the neo4j-graphrag library with a direct Python driver. You called retriever classes that handled embedding, vector search, and graph traversal internally. This lab connects to the same knowledge graph through the **Model Context Protocol (MCP)**, an open standard that defines how AI agents discover and interact with external tools and data sources. You'll progress from MCP basics to a fully autonomous Text2Cypher agent.

TIP: Ready to start? Jump to the xref:lab6-instructions.adoc[Lab 6 Instructions] for the notebook walkthrough.

.**At a Glance**

* **One notebook**: progress from MCP tool discovery to autonomous Cypher generation
* **Strands Agents SDK**: AWS-native agent framework with built-in MCP support
* **Text2Cypher pattern**: the agent reads the schema and writes original Cypher, then executes it over MCP
* **Schema-first approach**: the agent retrieves the graph schema before writing any query

== Model Context Protocol (MCP)

The Model Context Protocol is an open standard that defines how AI agents discover and interact with external tools and data sources. It provides a uniform interface: any MCP-compatible agent connects to any MCP-compatible server without custom integration code.

=== Three-Component Architecture

MCP defines a three-component architecture:

image::lab6-mcp-agent-architecture.png[MCP Agent Architecture,700]

**AI Agent (Client)**: The LLM-powered application that needs to access external data. It discovers available tools by asking the MCP server what operations are supported.

**MCP Server**: A lightweight service that exposes data source operations as tools. It translates between the MCP protocol and the data source's native API.

**Data Source**: The underlying system (Neo4j, a REST API, a file system, etc.) that holds the data.

=== Transport Options

MCP supports multiple transport mechanisms:

[cols="1,2,2"]
|===
| Transport | Description | Use Case

| **stdio**
| Standard input/output streams
| Local development, CLI tools

| **Streamable HTTP**
| HTTP-based streaming protocol
| Cloud deployments, remote servers
|===

This lab uses Streamable HTTP to connect to an MCP Gateway running on AWS.

=== Neo4j MCP Server Tools

The https://github.com/neo4j/mcp[Neo4j MCP Server] is Neo4j's official MCP server. It requires APOC (a standard Neo4j plugin for schema introspection and utility procedures). In this workshop it is deployed in read-only mode, exposing two tools:

[cols="1,3"]
|===
| Tool | Description

| **`get_neo4j_schema`**
| Uses APOC's `apoc.meta.schema()` to read the graph schema, then simplifies the output to just property names and types (e.g., `"title": "STRING"`). This token-efficient format gives the LLM the structural information it needs for Text2Cypher without wasting context on APOC's full metadata.

| **`read_neo4j_cypher`**
| Executes a read-only Cypher query. Before running the query, the server performs an `EXPLAIN` round-trip to verify it contains no write operations (CREATE, MERGE, DELETE, SET). This is a server-side guard independent of the system prompt's instructions.
|===

The agent discovers these tools automatically through the MCP protocol. It does not need to know about them in advance.

NOTE: When accessed through the gateway, tool names are prefixed with the gateway target name and a `___` separator, for example `neo4j-mcp-server-target___get_neo4j_schema`. The canonical names from the server itself are `get_neo4j_schema` and `read_neo4j_cypher`. The server also provides `write_neo4j_cypher`, disabled here because the deployment runs in read-only mode (`NEO4J_READ_ONLY=true`).

== AWS Deployment Architecture

The MCP infrastructure is pre-deployed on AWS:

----
Agent (Notebook)  -->  AgentCore Gateway (HTTPS)  -->  Neo4j MCP Server (Container)  -->  Secrets Manager  -->  Neo4j Aura
----

**Agent (Notebook)**: Your Jupyter notebook running a Strands agent. It connects to the gateway using a URL and access token from `CONFIG.txt`.

**AgentCore Gateway**: An AWS-managed HTTPS endpoint that authenticates requests and routes them to the MCP server.

**Neo4j MCP Server**: Neo4j's official MCP server, deployed here in read-only mode over Streamable HTTP transport. It translates `get_neo4j_schema` and `read_neo4j_cypher` tool calls into Neo4j operations and tags every query with `MCP4NEO4J` metadata so database admins can identify MCP-originated traffic.

**Secrets Manager**: Stores the Neo4j Aura connection credentials securely. The MCP server retrieves them at runtime.

**Neo4j Aura**: Your graph database containing SEC financial data.

You do not need to set up any of this infrastructure. The gateway URL and access token are provided in `CONFIG.txt`.

The deployment code for this MCP server (AgentCore Gateway, IAM, container, and setup scripts) lives in the https://github.com/neo4j-partners/aws-starter[neo4j-partners/aws-starter] repository, in the `neo4j-agentcore-mcp-server/` directory. The lab administrator runs that deployment and distributes the resulting credentials.

== GraphRAG Retrieval Patterns

Neo4j's https://graphrag.com/reference/[GraphRAG Patterns Catalog] defines a spectrum of retrieval strategies, from controlled to autonomous:

[cols="1,2,1"]
|===
| Pattern | How It Works | Reliability

| **Cypher Templates**
| Pre-written, parameterized queries the agent selects and executes. Reliable because the queries are reviewed in advance.
| High (expert-reviewed queries)

| **Text2Cypher**
| The agent reads the schema, reasons about the question, and writes original Cypher from scratch. Flexible enough to answer any question the schema supports.
| Variable (depends on LLM reasoning quality)
|===

You already built the Cypher Templates end of the spectrum in xref:lab3.adoc[Lab 3] and xref:lab4.adoc[Lab 4], where `VectorCypherRetriever` ran a pre-written retrieval query and the Strands agent selected which retriever `@tool` to call. This lab covers the autonomous end: **Text2Cypher**, delivered over MCP. The trade-off is that Text2Cypher's query quality depends on the LLM's ability to reason about graph structures, which is why the schema-first approach is critical.

== Schema-First Approach

This lab's agent follows a schema-first pattern:

1. **Retrieve the schema** before writing any Cypher query. The agent calls `get_neo4j_schema` to learn what node labels, relationship types, and properties exist in the database.
2. **Write a Cypher query** based on the actual schema. This prevents the agent from guessing at property names or relationship types that do not exist.
3. **Execute the query** using `read_neo4j_cypher` and return the results.

This approach produces more accurate queries because the agent works with the real structure of the data rather than assumptions.

.How schema-first prevents query failures
[%collapsible]
====
Without the schema step, an LLM might generate `MATCH (c:Corp)-[:HAS_PRODUCT]->(p:Product)` when the actual label is `Company` and the relationship type is `OFFERS`. The query executes without error but returns zero results because `Corp` and `HAS_PRODUCT` do not exist in the graph. The agent would then report "no data found" as a confident answer.

With schema retrieval, the agent sees the actual labels (`Company`, `Product`, `RiskFactor`, `AssetManager`) and relationship types (`OFFERS`, `FACES_RISK`, `COMPETES_WITH`, `PARTNERS_WITH`, `OWNS`, `FILED`, `FROM_DOCUMENT`) before writing any query. The generated Cypher uses the correct vocabulary, and empty results genuinely mean no matching data rather than a query targeting non-existent schema elements.
====

== Why a Specialized Graph Agent

An agent that handles both SQL analytics and graph traversal in a single prompt must hold two query languages, two data models, and two sets of conventions in context simultaneously. SQL operates on rows, filters, and aggregations. Cypher operates on paths, patterns, and traversals. Mixing both dilutes the agent's focus: it may generate Cypher with SQL idioms (using WHERE clauses where pattern matching would be more precise) or SQL with graph assumptions (expecting JOIN-free traversals that relational databases cannot provide).

The architecture in this workshop separates concerns. The MCP agent in this lab handles only graph queries. It inspects the Neo4j schema, writes Cypher, and executes it. It does not need to know about SQL tables, aggregation functions, or relational joins. That focus produces better Cypher because the agent's entire context is graph-shaped.

In production systems, a supervisor or router agent sits above specialized agents and directs questions to the right one based on the data shape the question targets. Relationship traversals go to the graph agent. Time-series aggregations go to a SQL agent. Questions that span both get decomposed into sub-tasks routed to each specialist.

== Alternative Frameworks

This lab uses the **Strands Agents SDK**: AWS-native, built-in MCP support, simpler API. **LangGraph** is a viable alternative that provides fine-grained control over the agent loop via LangChain MCP adapters, better suited for complex multi-step workflows where you need to customize how the agent reasons, retries, or chains operations.

== Next Steps

Continue to xref:lab6-instructions.adoc[Lab 6 Instructions] for the notebook walkthrough.

To compare this MCP integration against the direct-driver approach, revisit xref:lab3.adoc[Lab 3], which uses the neo4j-graphrag library with the Python driver, and xref:lab2.adoc[Lab 2], which builds the retrieval flow from scratch.
