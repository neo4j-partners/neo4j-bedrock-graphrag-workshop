= Lab 1: Explore the Knowledge Graph

With the graph loaded in Lab 0, Lab 1 is where you get to know the data. You will query it with Cypher and explore it visually with Neo4j Explore. Understanding what the graph contains and how its two layers connect is the foundation for the GraphRAG labs: Lab 3 builds semantic search and GraphRAG retrieval over it, Lab 4 wraps that in a Strands agent, and Lab 6 connects a Neo4j MCP agent. The chunk embeddings those labs rely on were loaded in Lab 0.

TIP: Ready to start? Jump to xref:lab1-explore.adoc[Explore the Knowledge Graph] for the step-by-step Neo4j Explore walkthrough, and see xref:sample-queries.adoc[Sample Cypher Queries] for queries to run.

.**At a Glance**

* **Graph databases**: store data as nodes and relationships, where connections are explicit and traversable rather than hidden in foreign keys and joins
* **Cypher**: Neo4j's pattern-matching query language, where queries read like the graph patterns they match
* **The SEC Financial Knowledge Graph**: core entity types (Company, Product, RiskFactor, AssetManager) connected by typed relationships
* **Two layers**: a structured entity graph plus an unstructured document layer of chunks and embeddings, joined by the FILED relationship

== Graph Databases

A graph database models data as nodes and relationships. Nodes represent entities. Relationships represent the connections between them. Both carry properties, which are key-value pairs that describe their attributes.

The notation follows a visual convention. Parentheses denote nodes, brackets denote relationships:

----
(:Company {name, ticker})-[:FACES_RISK]->(:RiskFactor)
----

Each Company node carries properties like name and ticker. Each FACES_RISK relationship carries details like severity. The relationship is directional, typed, and stored as a first-class element alongside the nodes it connects.

.How graphs compare to relational databases
[%collapsible]
====
Relational databases represent connections implicitly through foreign keys and join tables. To answer "Which risk factors expose BlackRock's portfolio?", a relational query joins through an ownership junction table to companies, then through a risk factor junction table to risk factors, grouping and counting across both joins. Each hop requires another JOIN clause, and the query structure obscures the question it answers.

In a graph, the same question follows the connections directly:

[source,cypher]
----
MATCH (am:AssetManager {name: 'BlackRock'})-[:OWNS]->(c:Company)-[:FACES_RISK]->(r:RiskFactor)
RETURN r.name, count(DISTINCT c) AS exposedCompanies
ORDER BY exposedCompanies DESC
----

The Cypher reads like the traversal pattern it executes: start at BlackRock, follow OWNS to companies, follow FACES_RISK to risk factors, count how many companies share each risk. The graph does not need join tables because the connections are stored as first-class elements alongside the entities they link.
====

Cypher is the query language for interacting with Neo4j. It uses a pattern-matching syntax that mirrors the visual structure of nodes and relationships:

[source,cypher]
----
MATCH (c:Company {ticker: 'NVDA'})-[:OFFERS]->(p:Product)
RETURN p.name ORDER BY p.name LIMIT 10
----

.How the query works
[%collapsible]
====
`MATCH` finds nodes and relationships that fit the specified pattern. `(c:Company {ticker: 'NVDA'})` binds a Company node with the property `ticker = 'NVDA'` to the variable `c`. The `-[:OFFERS]\->` arrow follows outbound OFFERS relationships to `(p:Product)` nodes. `RETURN` selects which properties to include in the result set, and `LIMIT` caps the output. Cypher handles multi-hop traversals and path-finding operations natively.
====

.Why constraints matter for data loading
[%collapsible]
====
Lab 0 creates uniqueness constraints before loading any data. These constraints serve two purposes. First, they ensure that each entity has a unique identifier (Company by ticker, Product by name, and so on), preventing duplicate nodes from appearing in the graph. Second, they enable MERGE operations during data loading. MERGE creates a node if it does not exist and matches it if it does, making the load operation idempotent. Without the constraint, MERGE would need to scan all nodes of a label to check for existence, which is slow. With the constraint, Neo4j uses the index to perform the check efficiently.
====

== The SEC Financial Knowledge Graph

The workshop builds a knowledge graph from SEC 10-K financial filings. Core entity types and the relationships between them capture the structure of public companies, their products, risk factors, and institutional ownership:

[cols="1,3"]
|===
| Entity Type | Examples

| **Company**
| NVIDIA, Microsoft, Apple, Amazon, Tesla, JPMorgan Chase, Goldman Sachs, Johnson & Johnson, Pfizer

| **Product**
| GPUs, Cloud Services, iPhone, AWS, Model S, Investment Banking

| **RiskFactor**
| Supply Chain Disruption, Regulatory Compliance, Cybersecurity Threats

| **AssetManager**
| BlackRock, Vanguard, State Street, Fidelity
|===

These entities connect through typed relationships that reflect real-world structure:

----
(Company)-[:OFFERS]->(Product)
(Company)-[:FACES_RISK]->(RiskFactor)
(Company)-[:COMPETES_WITH]->(Company)
(Company)-[:PARTNERS_WITH]->(Company)
(AssetManager)-[:OWNS {shares}]->(Company)
----

A question like "Which risk factors expose an asset manager's portfolio across multiple companies?" requires traversing from AssetManager through OWNS to Company, then FACES_RISK to RiskFactor, grouping by risk factor. That multi-hop traversal is what the graph handles natively.

.Why this schema design
[%collapsible]
====
Company is the central node because most relationships radiate from it: companies offer products, face risks, compete with each other, partner with each other, and are owned by asset managers. This hub structure means most interesting questions start at or pass through a Company node.

COMPETES_WITH and PARTNERS_WITH are separate relationship types rather than a generic RELATES_TO with a type property. Separate types let Cypher filter by relationship type during traversal, which is indexed and fast. A property filter on a generic relationship requires checking every connection. When traversal patterns follow specific relationship types (as competitive analysis and partnership mapping do), typed relationships are the right design choice.
====

== The Two-Layer Graph

The graph has two layers, joined by the FILED relationship.

**The structured layer** holds the entity types above: Company, Product, RiskFactor, AssetManager, Document, and FinancialMetric, connected by typed relationships.

**The unstructured layer** connects those entities to the text of the filings. A Company node links to its filed Documents via FILED. Each Document links to its Chunks via FROM_DOCUMENT, and adjacent Chunks link to each other via NEXT_CHUNK to preserve reading order. Each Chunk carries an embedding vector, and the `chunkEmbeddings` vector index (1024 dimensions, from Amazon Titan Text Embeddings V2) supports semantic search over the chunk text. These embeddings were loaded in Lab 0.

This structure supports GraphRAG retrieval: vector search finds relevant Chunks, then graph traversal follows FROM_DOCUMENT to the Document and FILED back to the Company, pulling in structured entity context alongside the matched text. Lab 3 and Lab 4 build on exactly this pattern.

== Next Steps

Continue to xref:lab1-explore.adoc[Explore the Knowledge Graph] for the Neo4j Explore walkthrough, and try the queries on the xref:sample-queries.adoc[Sample Cypher Queries] page. When you are ready to move into the coding labs, Part 2 begins with xref:part2-setup.adoc[Part 2 Setup].
