= Lab 4: Graph-Enriched Search

In Lab 3 you built an agent that calls simple tools. Now you connect an agent to your knowledge graph and explore what makes searching a knowledge graph different from searching a standalone vector database. This lab walks through three retrieval strategies that grow in sophistication — from basic vector search, to graph-enriched search that follows relationships to connected entities, to hybrid search that combines semantic and keyword matching.

TIP: Ready to start? Jump to the xref:lab4-instructions.adoc[Lab 4 Instructions] for step-by-step notebook walkthroughs.

.**At a Glance**

* **Graph-enriched search** — follow relationships from matched chunks to connected entities like documents, companies, products, and risk factors
* **Vector search** — embed a query and find semantically similar chunks
* **Hybrid search** — combine keyword-based fulltext search with vector similarity for broader recall

image::lab4-mcp-retrieval-architecture.png[Graph-Enriched Search Architecture,700]

== Why Graph-Enriched Search?

A standard vector search returns isolated text passages ranked by similarity. That is useful, but it throws away the structured knowledge surrounding each passage. When your data lives in a knowledge graph, every chunk of text is connected to the entities it describes.

Consider a query about supply chain risks. A vector search finds the most relevant chunk from a 10-K filing — a paragraph discussing logistics disruptions. Graph-enriched search returns that same chunk _plus_ the Company that filed it, the Products affected, other RiskFactors the company faces, and the Document the chunk came from. The agent receives structured context alongside the unstructured text, and can give a far richer answer.

This is what makes retrieval from a knowledge graph different. The graph turns text retrieval into knowledge retrieval.

== Chunks and Embeddings

Before diving into the retrieval strategies, two concepts you will see throughout the notebooks.

=== What Are Chunks?

SEC 10-K filings are long documents — too long to search as a single unit. Each filing is split into smaller passages called chunks. In the knowledge graph, each passage is a `Chunk` node linked to its source `Document` via a `FROM_DOCUMENT` relationship and to adjacent chunks via `NEXT_CHUNK`, preserving the original reading order. The chunking has already been done for you — the database is pre-loaded and ready to query.

=== What Are Embeddings?

An embedding is a numerical vector that captures the meaning of a piece of text. An embedding model (Bedrock Nova in this workshop) reads a chunk and produces a 1024-dimensional vector. Chunks about similar topics produce similar vectors, so "supply chain disruption" and "logistics bottleneck risk" end up close together in vector space. These vectors are stored on `Chunk` nodes and indexed for fast approximate nearest-neighbor search via the `chunkEmbeddings` vector index.

NOTE: The chunks, embeddings, and indexes were created by the workshop admin before the lab. You do not need to build the data pipeline yourself. xref:lab5.adoc[Lab 5] covers building it from scratch, including chunking trade-offs and embedding generation.

== Three Retrieval Strategies

The three notebooks progress through retrieval strategies that build on each other. Each strategy returns more context than the previous one. Vector search returns chunk text. Graph-enriched search returns chunk text plus connected entities and document metadata. Hybrid search returns results from both keyword and semantic matching, giving the agent the broadest possible context to reason over.

=== Vector Search

Notebook 01 introduces the basic pattern. You embed a natural-language query using Bedrock Nova, pass the embedding to the agent, and let the agent execute vector search Cypher. The vector index (`chunkEmbeddings`) finds the chunks whose embeddings are most similar to the query embedding. The results are isolated chunks ranked by similarity — no graph context yet.

=== Graph-Enriched Search

Notebook 02 extends vector search by adding graph traversal. After finding semantically similar chunks, the agent follows relationships to pull in document metadata, neighboring chunks (via `NEXT_CHUNK`), and connected entities — companies, products, and risk factors. This context enrichment gives the agent more information to work with when generating answers.

image::lab4-graph-enriched-retrieval.png[Graph-Enriched Retrieval Flow,700]

=== Hybrid Search

Notebook 03 introduces an alternative search method: fulltext keyword search using Lucene operators against the `search_chunks` and `search_entities` indexes. It then combines both approaches for agent-driven hybrid search using custom `@tool` wrappers that encapsulate embedding generation.

== How the Agent Connects: MCP

The agent communicates with Neo4j through MCP (Model Context Protocol), an open standard for agent-to-tool communication. The agent sends Cypher through an `execute-query` tool exposed by the Neo4j MCP Server. The server handles the connection, runs the query, and returns structured results. MCP is the transport layer for these queries, not the retrieval strategy itself. For a deeper look at MCP and building agents that write their own Cypher, see xref:lab6.adoc[Lab 6].

NOTE: This lab uses the https://github.com/neo4j/mcp[Neo4j MCP Server], an official MCP server that exposes a Neo4j database as a set of tools an agent can call — including `execute-query` for running Cypher, schema introspection, and index management. The server has been deployed to AgentCore using the https://github.com/neo4j-partners/aws-starter/tree/main/neo4j-agentcore-mcp-server[neo4j-agentcore-mcp-server starter] and connects to a database pre-loaded with Document and Chunk nodes, vector embeddings (`chunkEmbeddings` index), and fulltext indexes (`search_chunks`, `search_entities`). The workshop admin set this up before the workshop. You do not need to deploy the MCP server or create these data structures yourself — everything is ready to query.

== The @tool Pattern

All three strategies share the same underlying flow. The agent receives a natural-language question, decides which search to run, and calls a custom tool function. That tool function handles the mechanical work the LLM should never see: generating the embedding, building the Cypher query, and sending it to Neo4j through the MCP connection. The results come back as text that the agent can reason over and present to the user.

The important design choice is where the embedding lives. A 1024-dimensional vector is roughly 8–10KB of floating-point numbers. If that array passes through the LLM's context window, it wastes tokens on data the model cannot interpret, doubles the payload (once in the message, once in the tool call), and risks the model silently truncating or rounding values. Wrapping embedding generation inside a `@tool` function keeps the vector on the data plane. The agent calls `vector_search_tool("supply chain risks")` and receives chunk text and scores. It never touches the float array.

This is the same separation of concerns that the neo4j-graphrag library enforces in Lab 5: the `VectorRetriever` generates embeddings and queries the index internally, and the LLM only sees retrieved text. The `@tool` wrapper achieves the same boundary when working through MCP instead of a direct driver connection.

== Comparing Approaches: MCP vs Direct Library

All three notebooks use the same underlying Neo4j indexes and graph structure as Lab 5. The difference is in how the search is executed.

[cols="1,2,2"]
|===
| Aspect | Lab 4 (MCP) | Lab 5 (Library)

| **Connection**
| MCP server via AgentCore Gateway
| Direct Neo4j driver

| **Query execution**
| Agent calls `execute-query` MCP tool
| Retriever class methods

| **Embedding**
| Client-side (Bedrock Nova), passed to agent or wrapped in `@tool`
| Retriever calls embedder internally

| **Hybrid search**
| Agent runs both queries and combines results
| `HybridRetriever` handles score normalization and alpha tuning

| **Graph traversal**
| Cypher in agent prompt
| `retrieval_query` parameter on retriever classes
|===

== What Is Covered Here vs Lab 5

This lab focuses on graph-enriched search: using an agent to execute retrieval queries against a knowledge graph. For the following topics, see xref:lab5.adoc[Lab 5]:

* **Direct driver connections** — `neo4j.GraphDatabase.driver()` for direct access
* **neo4j-graphrag retriever classes** — `VectorRetriever`, `VectorCypherRetriever`, `HybridRetriever`, `HybridCypherRetriever`
* **Programmatic alpha tuning** — `HybridRetriever.search(alpha=0.5)` with automatic score normalization
* **GraphRAG pipeline** — `GraphRAG(llm=llm, retriever=retriever).search()` for end-to-end RAG
* **Data loading and embedding generation** — building the graph from scratch

== Next Steps

Continue to xref:lab4-instructions.adoc[Lab 4 Instructions] to work through the notebooks. After completing this lab, proceed to xref:lab5.adoc[Lab 5: GraphRAG] for direct library-based retrieval, or to xref:lab6.adoc[Lab 6: MCP Agent] to build full MCP agents.
