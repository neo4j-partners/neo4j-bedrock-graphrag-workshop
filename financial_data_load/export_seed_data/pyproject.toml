[project]
name = "export-seed-data"
version = "0.1.0"
description = "Export Neo4j graph data to financial_data_load/seed-data CSVs"
requires-python = ">=3.11"
dependencies = [
    "neo4j>=5.0.0",
    "python-dotenv>=1.0.0",
]

[tool.hatch.build.targets.wheel]
packages = ["."]
